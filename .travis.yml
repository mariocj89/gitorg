language: python
sudo: false
python:
      - "2.7"
      - "3.5"
install:
    - "pip install -r requirements.txt"
    - "pip install python-coveralls"
script:
    - "nosetests -v tests"
# report coverage to coveralls.io
after_success:
    - coveralls
